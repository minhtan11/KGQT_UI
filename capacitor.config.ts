import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'KGQT',
  appName: 'KGQT',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
